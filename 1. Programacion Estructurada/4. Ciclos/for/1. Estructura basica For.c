#include <stdio.h>
	//Ciclo For sintaxis basica
int main(){
	int i; 
	
	for(i=1; i<=10; i++){
		//Se toma en cuenta el 1 y se imprime 10 veces
		printf("El valor de mi variable iteradora es: %d \n", i); 
	}
	
		printf("Fin del bucle for");
		//Solo se imprime una sola vez ya que está a fuera del ciclo y no tiene un intedor en el 
	return 0; 
}

